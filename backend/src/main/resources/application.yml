server:
  port: ${PORT:8080}

spring:
  application:
    name: hr-api

  security:
    user:
      name: admin
      password: admin123
      roles: ADMIN

  datasource:
    url: jdbc:postgresql://localhost:5432/hr_namtienjsc_db
    username: postgres
    password: 2704

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: Asia/Ho_Chi_Minh

jwt:
  secret: ${JWT_SECRET:demo-secret-key-demo-secret-key-demo-secret-key-123456}
  expiration: 86400000

logging:
  level:
    com.namtienjsc.hr: DEBUG
